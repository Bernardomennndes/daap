{
  "name": "tools",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "tsc -b -f -clean"
  },
  "devDependencies": {
    "@daap/typescript-config": "workspace:*",
    "@daap/eslint-config": "workspace:*",
    "@types/node": "^20.19.19",
    "typescript": "^5.2.2",
    "eslint": "^8.57.0"
  }
}
